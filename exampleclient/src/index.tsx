import React from 'react';
import ReactDOM from 'react-dom';
import ExampleApp from './example';

import './App.css'

const rootElement = document.getElementById('example-container');
ReactDOM.render(<ExampleApp/>, rootElement);
